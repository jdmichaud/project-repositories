/*
** ftp4.c for complètement_local in 
** 
** Made by jean-daniel michaud
** Login   <michau_j@epita.fr>
** 
** Started on  Sat Dec 15 11:56:17 2001 jean-daniel michaud
** Last update Sat Dec 15 15:41:27 2001 jean-daniel michaud
*/

#include <stdio.h>

#include "struct.h"
#include "ftp.h"

int	stou(char **argv, t_server *server, t_user *user)
{
  noimp(NULL, server, user);
  return (0);
}

int	appe(char **argv, t_server *server, t_user *user)
{
  noimp(NULL, server, user);
  return (0);
}

int	allo(char **argv, t_server *server, t_user *user)
{
  noimp(NULL, server, user);
  return (0);
}

int	rest(char **argv, t_server *server, t_user *user)
{
  noimp(NULL, server, user);
  return (0);
}
